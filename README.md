# PlainMed
PlainMed explains lab reports in plain language, with every sentence linked to the report line that supports it. Numbers come from a deterministic parser, never the AI, and identifiers are removed before the model sees anything. Google MedGemma on NVIDIA GPUs. Nothing stored. Launching in India, five languages.
