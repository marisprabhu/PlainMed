# PlainMed Streamlit configuration.
# Offline-by-design: bind to localhost only, no usage statistics.

[server]
address = "127.0.0.1"
headless = true
maxUploadSize = 10
enableXsrfProtection = true
enableCORS = true

[browser]
gatherUsageStats = false
